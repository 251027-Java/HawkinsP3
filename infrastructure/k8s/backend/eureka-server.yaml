# Eureka Server - Service Discovery
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-server
  namespace: pilotquiz-backend
  labels:
    app: eureka-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eureka-server
  template:
    metadata:
      labels:
        app: eureka-server
    spec:
      containers:
        - name: eureka-server
          image: richardhawkins/pilotquiz-eureka-server:latest
          ports:
            - containerPort: 8761
          resources:
            limits:
              memory: "384Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "100m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8761
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8761
            initialDelaySeconds: 60
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: eureka-server
  namespace: pilotquiz-backend
spec:
  selector:
    app: eureka-server
  ports:
    - port: 8761
      targetPort: 8761
